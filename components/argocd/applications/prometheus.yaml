apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: prometheus-mgt-hsk
  namespace: argocd
spec:
  project: default
  sources:
  - repoURL: git@github.com:marranz/k8s-local-env.git
    path: management/hsk/helm/prometheus/extra-manifests/
    name: extra-manifests

  - repoURL: https://prometheus-community.github.io/helm-charts/
    chart: prometheus
    targetRevision: v27.20.0
    helm:
      valueFiles:
        - $values/management/hsk/helm/prometheus/helm-values.yaml

  - repoURL: 'git@github.com:marranz/k8s-local-env.git
    ref: values

  destination:
    server: https://kubernetes.default.svc
    namespace: prometheus

  syncPolicy:
    automated:
      prune: false
      selfHeal: false
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
